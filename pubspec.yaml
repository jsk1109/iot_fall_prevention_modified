name: iot_fall_prevention
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- HTTP & 상태 관리 ---
  http: ^1.2.1
  provider: ^6.1.1
  
  # --- 차트 및 UI ---
  fl_chart: ^0.68.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.2

  # --- 실시간 통신 (신규) ---
  web_socket_channel: ^2.4.0

  # --- 기타 유틸리티 ---
  connectivity_plus: ^5.0.2

  # --- Amplify (기존 의존성 유지) ---
  amplify_flutter: ^1.8.0
  amplify_auth_cognito: ^1.8.0
  amplify_core: ^1.8.0
  firebase_core: ^3.4.0
  firebase_messaging: ^15.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

